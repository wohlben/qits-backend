# Tests run against in-memory H2 instead of Postgres, so the suite needs no
# Docker/Testcontainers. Production still uses Postgres (see application.properties).
quarkus.datasource.db-kind=h2
quarkus.datasource.jdbc.url=jdbc:h2:mem:qits;DB_CLOSE_DELAY=-1

quarkus.flyway.clean-at-start=true
