# Dev mode runs against in-memory H2 so `quarkus:dev` needs no Docker/Postgres
# (mirrors the test setup). Production still uses Postgres (see application.properties).
quarkus.datasource.db-kind=h2
quarkus.datasource.jdbc.url=jdbc:h2:mem:qits;DB_CLOSE_DELAY=-1
