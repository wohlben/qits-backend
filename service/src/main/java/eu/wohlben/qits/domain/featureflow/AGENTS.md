# Domain: Feature Flow

The Feature Flow domain models the lifecycle of feature development. A *feature flow* is configured generally, and the **Action** domain will eventually be merged into it. The main entity is **FeatureFlowConfiguration**, which provides the blueprint for initiated feature developments.
