# Domain: Action

The Action domain manages executable task definitions through **action-configurations**. Each configuration defines a shell script to execute and a companion check script that self-reports whether execution is necessary.

