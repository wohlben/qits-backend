# Propose Change

## User Story

As a developer, I want to start work on a change in a repository by forking a new branch from a chosen parent branch, so that I can modify the codebase in isolation without disrupting the parent branch.

Each change gets its own branch — two changes never share a branch — so concurrent work cannot collide. This change may be part of a feature flow or initiated ad-hoc by a developer.

## Builds On

1. `repository/onboard-codebase`

## Processes

- `repositories-repoId-branches`
- `repositories-repoId-worktrees`
