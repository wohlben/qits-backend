# Project

A project defines a scope boundary that contextualizes multiple repositories together. Within a project, repositories share action configurations and feature flows, allowing consistent quality gates and lifecycle pipelines across related codebases.

A project does not represent a team or an organizational unit; it represents a domain of shared context.
