# Repository

A repository is a version-controlled codebase that serves as the technical substrate for feature flows. It is not a domain of its own; it enables the project and feature-flow domains by providing the code that moves through a pipeline.

Repositories are always created within a project scope. They are onboarded into QITS so that worktrees can be created and the project's shared feature flows can be executed against them.
