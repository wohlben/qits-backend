# Committed screenshot baselines fail under a freshly installed Playwright Chromium

## Introduction

Encountered while adding the session-tree screenshot test for
[embedded-workspace-agent-session](../features/2026-07-10_embedded-workspace-agent-session.md);
the visual-test setup itself is documented in `.pi/skills/screenshot-tests/SKILL.md`. Unrelated to
that feature's change — the failing specs render components it doesn't touch.

## Observed repro

On a machine without a Playwright browser cache (`pnpm exec playwright install chromium` pulls
Chrome Headless Shell 149.0.7827.55, playwright 1.61.0):

```bash
cd service/src/main/webui
pnpm test:visual
```

Four pre-existing tests fail on pixel drift against their committed baselines
(`Matcher did not succeed in time.`):

- `src/app/ui/components/repository/commit-row.browser.spec.ts` (1)
- `src/app/ui/components/repository/branch-tree.browser.spec.ts` (1)
- `src/app/ui/components/file-viewer/file-viewer.browser.spec.ts` (2, light + dark)

A baseline generated by this same Chromium (the new
`agent-session-rows.browser.spec.ts` → `session-tree-chromium-linux.png`) passes, so the harness
works — the committed PNGs just came from a different Chromium build (and possibly different
system fonts), and text rasterization drifted.

## Suspected cause

Screenshot baselines are only reproducible for the exact browser build + font stack that produced
them. The repo pins neither: `playwright` floats within its semver range, and the baselines don't
record which Chromium rendered them. Any environment that installs a newer Chromium regenerates
different pixels. (The skill's debugging table already notes the sibling issue of
timezone-dependent `DatePipe` output.)

## Suggested fix direction

- Regenerate the four stale baselines on the current pinned toolchain and commit them together
  with a note of the Chromium version (or bump/pin `playwright` so every environment resolves the
  same browser build).
- Consider a devcontainer-baked browser install (the `docker/workspace` image could pre-install
  the Playwright cache) so "fresh machine" and "CI" always mean the same renderer.
